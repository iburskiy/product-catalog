    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 elem">
        <div class="col-lg-4 col-md-4 col-sm-12 col-xs-12">
            <a href="/#about/query/{{href}}" title="{{manufacturer}} {{model}}" class="link-item"><img src="{{img0}}"></a>
        </div>
        <div class="col-lg-8 col-md-8 col-sm-12 col-xs-12">
            <strong><a href="/#about/query/{{href}}" title="{{manufacturer}} {{model}}" class="link-item">{{manufacturer}} {{model}}</a></strong>
            <p>{{cpu}} {{platform}} {{type}} {{ram}} {{color}} </p>
        </div>
    </div>

